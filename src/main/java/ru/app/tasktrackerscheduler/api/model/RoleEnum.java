package ru.app.tasktrackerscheduler.api.model;

/**
 * Enumeration representing user roles.
 * Defines roles that can be assigned to users in the application.
 */
public enum RoleEnum {
    /**
     * Role representing a regular user.
     */
    USER,

    /**
     * Role representing an administrator user with elevated privileges.
     */
    ADMIN
}
